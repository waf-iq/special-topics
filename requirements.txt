# Core
numpy>=1.26
pandas>=2.1
pyarrow>=15.0
pyyaml>=6.0

# Embeddings
sentence-transformers>=2.5
torch>=2.1

# Retrieval
scikit-learn>=1.4
rank-bm25>=0.2.2
qdrant-client>=1.18,<1.19  # keep within 1 minor of the qdrant/qdrant:v1.18.2 server image

# Storage / graph (D2)
pymongo>=4.6
neo4j>=5.0

# Serving (D2)
fastapi>=0.110
uvicorn[standard]>=0.29

# AutoML
optuna>=3.5
mlflow>=2.10

# Online learning
river>=0.21

# Ingest
pypdf>=4.0
pymupdf>=1.23
requests>=2.31
arxiv>=2.1
ir_datasets>=0.5

# Eval & viz
matplotlib>=3.8
tabulate>=0.9          # pandas .to_markdown() backend, used by mlflow_tracking export

# Testing
pytest>=8.0
httpx>=0.27          # fastapi.testclient.TestClient backend (tests/test_api.py)

# Notebook
jupyter>=1.0
ipykernel>=6.29

# D3 — GraphRAG answering & eval (not imported by the H0 scaffold; needed by D3-4 ceiling + D3-5)
openai>=1.30          # single OpenAI-compatible client for the answerer (Ollama local + Groq)
ragas>=0.2            # faithfulness + answer-relevance
langchain-groq>=0.2   # free Groq judge / answerer-ceiling (llama-3.3-70b-versatile)
langchain-huggingface>=0.1  # HuggingFace embeddings backend for RAGAS
groq>=0.11
# Local graded answerer (D4 QLoRA target): Qwen2.5-3B-Instruct via Ollama (external) or transformers (already a dep)
